# Desarrollo local (opcional — por defecto usa http://localhost:3001)
# VITE_API_URL=http://localhost:3001

# Producción en Vercel (opcional — por defecto usa /api del mismo dominio)
# VITE_API_URL=/api
